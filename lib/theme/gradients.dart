import 'package:flutter/material.dart';

const blueGradient = LinearGradient(
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
  tileMode: TileMode.clamp,
  stops: [0.0, 1],
  colors: [
    Color(0xFF20D1FF),
    Color(0xFF1286FF),
  ],
);
const blueChangeDateGradient = LinearGradient(
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
  tileMode: TileMode.clamp,
  stops: [0.0, 1],
  colors: [
    Color(0xFF20D1FF),
    Color(0xFF1286FF),
  ],
);

const purpelBlueGradient = LinearGradient(
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
  tileMode: TileMode.clamp,
  stops: [0.0, 1],
  colors: [
    Color(0xFF9269F4),
    Color(0xFF20D1FF),
  ],
);

const pinkPurpleGradient = LinearGradient(
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
  tileMode: TileMode.clamp,
  stops: [0.0, 1],
  colors: [
    Color(0xFFE972E6),
    Color(0xFF8568F6),
  ],
);

const yellowOrangeGradient = LinearGradient(
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
  tileMode: TileMode.clamp,
  stops: [0.0, 1],
  colors: [
    Color(0xFFFDC661),
    Color(0xFFF68661),
  ],
);

const blueDarkBlueGradient = LinearGradient(
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
  tileMode: TileMode.clamp,
  stops: [0.0, 1],
  colors: [
    Color(0xFF20D1FF),
    Color(0xFF1286FF),
  ],
);

const redGradient = LinearGradient(
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
  tileMode: TileMode.clamp,
  stops: [0.0, 1],
  colors: [
    Color(0xFFFF5964),
    Color(0xFFFF3340),
  ],
);

const greyGradient = LinearGradient(
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
  tileMode: TileMode.clamp,
  stops: [0.0, 1],
  colors: [
    Color(0xFFB0BDD0),
    Color(0xFFB0BDD0),
  ],
);

const greenGradient = LinearGradient(
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
  tileMode: TileMode.clamp,
  stops: [0.0, 1],
  colors: [
    Color(0xFF59CD90),
    Color(0xFF11B55F),
  ],
);
